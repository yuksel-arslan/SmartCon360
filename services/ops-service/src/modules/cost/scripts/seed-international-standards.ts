// Seed International Standards — DEPRECATED
// MasterFormat and UNIFORMAT II have been removed from the platform.
// Classification standards are now: Uniclass 2015 + OmniClass + Custom only.
//
// This script is kept as a no-op placeholder. Previously seeded data
// in the database can be manually cleaned up if needed.

async function main() {
  console.log('ℹ️  MasterFormat and UNIFORMAT II seeding has been deprecated.');
  console.log('   SmartCon360 now uses Uniclass 2015 + OmniClass + Custom only.');
  console.log('   No action taken.');
}

main();
