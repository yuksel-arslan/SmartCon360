# SmartCon360 — Environment Variables
DATABASE_URL=postgresql://taktflow:taktflow_dev@localhost:5432/taktflow
REDIS_URL=redis://localhost:6379
JWT_SECRET=dev-secret-smartcon360-2026
CORS_ORIGINS=http://localhost:3100
# Production: set CORS_ORIGINS to your Vercel frontend URL
LOG_LEVEL=info

# ── Platform Service URLs (for api-gateway) ──
# Local development uses localhost, Railway uses internal service networking
AUTH_URL=http://localhost:3001
PROJECT_URL=http://localhost:3002
TAKT_URL=http://localhost:8001
AI_PLANNER_URL=http://localhost:8002
FLOWLINE_URL=http://localhost:3003
CONSTRAINT_URL=http://localhost:3004
PROGRESS_URL=http://localhost:3005
SIMULATION_URL=http://localhost:8003
RESOURCE_URL=http://localhost:3006
NOTIFICATION_URL=http://localhost:3007
REPORTING_URL=http://localhost:8004
BIM_URL=http://localhost:8005
CONCIERGE_URL=http://localhost:3008
ANALYTICS_URL=http://localhost:8006

# ── New Module Service URLs ──
QUALITY_URL=http://localhost:3009
SAFETY_URL=http://localhost:3010
COST_URL=http://localhost:3011
CLAIMS_URL=http://localhost:3012
SUPPLY_CHAIN_URL=http://localhost:3013
RISK_URL=http://localhost:3014
COMM_URL=http://localhost:3015
STAKEHOLDER_URL=http://localhost:3016
SUSTAINABILITY_URL=http://localhost:3017
VISION_URL=http://localhost:8008
HUB_URL=http://localhost:3018

# Railway — set these in Railway dashboard per service
# RAILWAY_PUBLIC_DOMAIN= (auto-assigned)
# RAILWAY_PRIVATE_DOMAIN= (internal networking)

# AI
GEMINI_API_KEY=
AI_MODEL=gemini-2.5-pro

# ── Cloud Storage (S3-compatible) ──
# Supports AWS S3, Cloudflare R2, MinIO, DigitalOcean Spaces
# Set STORAGE_PROVIDER=s3 to enable cloud storage (default: local = database)
STORAGE_PROVIDER=local
# STORAGE_BUCKET=smartcon360-files
# STORAGE_REGION=auto
# STORAGE_ENDPOINT=                     # Custom endpoint (R2: https://<account>.r2.cloudflarestorage.com)
# STORAGE_ACCESS_KEY_ID=
# STORAGE_SECRET_ACCESS_KEY=
# STORAGE_PUBLIC_URL=                   # Public URL prefix if bucket has public access
# STORAGE_FORCE_PATH_STYLE=false        # Set true for MinIO

# Frontend (Vercel) — set in Vercel dashboard, not Railway
# NEXT_PUBLIC_API_URL=https://<railway-api-gateway-domain>/api/v1
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=  # Google OAuth 2.0 Client ID (from Google Cloud Console)

# ── Production Domain ──
# Custom domain configured in Vercel: app.smartcon360.com
# All *.vercel.app URLs auto-redirect to https://app.smartcon360.com
# Google OAuth authorized origin: https://app.smartcon360.com
