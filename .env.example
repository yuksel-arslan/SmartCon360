# SmartCon360 — Environment Variables

DATABASE_URL=postgresql://smartcon360:smartcon360_dev@localhost:5432/smartcon360
REDIS_URL=redis://localhost:6379
JWT_SECRET=dev-secret-smartcon360-2026
CORS_ORIGINS=http://localhost:3100
LOG_LEVEL=info

# ── Service URLs (for api-gateway) ──
CORE_SERVICE_URL=http://localhost:3001
OPS_SERVICE_URL=http://localhost:3002
PLATFORM_SERVICE_URL=http://localhost:3003
TAKT_SERVICE_URL=http://localhost:8001
AI_SERVICE_URL=http://localhost:8002

# ── AI ──
GEMINI_API_KEY=
AI_MODEL=gemini-2.5-pro

# ── Cloud Storage ──
# STORAGE_PROVIDER options:
#   local        = PostgreSQL database (development, max 20 MB)
#   google-drive = Project owner's Google Drive (requires GOOGLE_CLIENT_SECRET)
#   s3           = S3-compatible bucket (AWS S3, Cloudflare R2, MinIO)
STORAGE_PROVIDER=google-drive
# STORAGE_BUCKET=smartcon360-files
# STORAGE_REGION=auto
# STORAGE_ENDPOINT=                     # Custom endpoint (R2: https://<account>.r2.cloudflarestorage.com)
# STORAGE_ACCESS_KEY_ID=
# STORAGE_SECRET_ACCESS_KEY=
# STORAGE_PUBLIC_URL=                   # Public URL prefix if bucket has public access
# STORAGE_FORCE_PATH_STYLE=false        # Set true for MinIO

# ── Frontend (Vercel) ──
# NEXT_PUBLIC_API_URL=https://<railway-api-gateway-domain>/api/v1
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=  # Google OAuth 2.0 Client ID (from Google Cloud Console)
# GOOGLE_CLIENT_SECRET=          # Google OAuth 2.0 Client Secret (required for Drive storage)

# ── Production Domain ──
# Custom domain configured in Vercel: app.smartcon360.com
# All *.vercel.app URLs auto-redirect to https://app.smartcon360.com
# Google OAuth authorized origin: https://app.smartcon360.com
